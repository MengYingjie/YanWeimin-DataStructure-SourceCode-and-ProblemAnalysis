#include <stdio.h>
int main(int argc, char *argv[])
{
	int i, j, k;
	
	for(i=0; i<=5; i++)
	{
		for(j=0; j<=5; j++)
		{
			for(k=0; k<=5; k++)
			{
				if((i+j+k)==8)
					printf("i=%d j=%d k=%d\n", i, j, k);
			}
		}
	}
	
	return 0;
}

